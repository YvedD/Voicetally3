<?xml version="1.0" encoding="utf-8"?>
<!--
 Dialooglayout:
 - Titel
 - MapView in vaste hoogte (volwaardige gestures)
 - Resultaten-tekst in een eigen ScrollView met vaste hoogte
 - Actieknoppen onderaan
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/resultsDialogRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginBottom="30dp"
    android:background="@android:color/black"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- Kop -->
    <TextView
        android:id="@+id/txtHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="8dp"
        android:text="Resultaten"
        android:textColor="@android:color/white"
        android:textSize="18sp"
        android:textStyle="bold" />

    <!-- Kaart (vaste hoogte) -->
    <FrameLayout
        android:id="@+id/mapHolder"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:layout_marginBottom="8dp">

        <org.osmdroid.views.MapView
            android:id="@+id/mapViewResults"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    <!-- Resultaten-tekst in eigen scrollgebied (vaste hoogte) -->
    <ScrollView
        android:id="@+id/resultsScroll"
        android:layout_width="match_parent"
        android:layout_height="240dp"
        android:layout_marginBottom="8dp"
        android:fadeScrollbars="false"
        android:fillViewport="true">

        <TextView
            android:id="@+id/txtResults"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingExtra="2dp"
            android:textColor="@android:color/white"
            android:textSize="14sp" />
    </ScrollView>

    <!-- Knoppen -->
    <LinearLayout
        android:id="@+id/buttonsRow"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnOpslaanDelenReset"
            style="@style/Widget.VoiceTally.Button"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Opslaan + Delen + Reset"
            android:textSize="10dp" />

        <View
            android:layout_width="8dp"
            android:layout_height="1dp" />

        <Button
            android:id="@+id/btnNieuweSessie"
            style="@style/Widget.VoiceTally.Button"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="Nieuwe sessie"
            android:textSize="10dp" />
    </LinearLayout>

</LinearLayout>
